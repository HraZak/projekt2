<div class="nav justify-content-end mt-3">
<a class="btn btn-primary me-3" href="../prohlizec.php" role="button">Úvod</a>
    <div class="dropdown">
  <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
    {{login}}
  </button>
  <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
    <li><a class="dropdown-item" href="../zmenaHesla.php">Změnit heslo</a></li>
    <li><a class="dropdown-item" href="../logout.php">Odhlášení</a></li>
  </ul>
    </div>
</div>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Name</th>
        <th>Number</th>
        <th>Phone</th>
        {{#admin}}
        <th>Actions</th>
        {{/admin}}
    </tr>
    </thead>

    <tbody>
    {{# rooms }}
        <tr>
            <td><a href="./room.php?room_id={{ room_id }}">{{ name }}</a></td>
            <td>{{ no }}</td>
            <td>{{ phone }}</td>
            {{#admin}}
            <td>
                <form method="post" action="./update.php">
                    <input type="hidden" name="room_id" value="{{room_id}}" />
                    <input type="submit" value="Update" class="btn btn-info"/>
                </form>
                <form method="post" action="./delete.php" onsubmit="return confirm('Really delete room?\nAction cannot be undone!')">
                    <input type="hidden" name="room_id" value="{{room_id}}" />
                    <input type="submit" value="Delete" class="btn btn-danger"/>
                </form>
            </td>
            {{/admin}}
        </tr>
    {{/ rooms }}
    </tbody>
</table>
{{#admin}}
<a href="./insert.php" class="btn btn-primary">New Room</a>
{{/admin}}
