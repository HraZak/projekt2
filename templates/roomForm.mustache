<div class="nav justify-content-end mt-3">
<a class="btn btn-primary me-3" href="../prohlizec.php" role="button">Úvod</a>
    <div class="dropdown">
  <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
    {{login}}
  </button>
  <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
    <li><a class="dropdown-item" href="../zmenaHesla.php">Změnit heslo</a></li>
    <li><a class="dropdown-item" href="../logout.php">Odhlášení</a></li>
  </ul>
    </div>
</div>

<h1>New room</h1>
<form method="post">
    <div class="mt-3">
    <label for="name">Name</label>
        <input type="text" name="name" id="name" value="{{room.name}}" class="form-control"/>
        {{#errors.name}}<small class="text-warning">{{errors.name}}</small>{{/errors.name}}
    </div>

    <div class="mt-3">
    <label for="no">No.</label>
        <input type="text" name="no" id="no" value="{{room.no}}" class="form-control"/>
        {{#errors.no}}<small class="text-warning">{{errors.no}}</small>{{/errors.no}}
    </div>

    <div class="mt-3">
    <label for="phone">Phone</label>
        <input type="text" name="phone" id="phone" value="{{room.phone}}" class="form-control"/>
        {{#errors.phone}}<small class="text-warning">{{errors.phone}}</small>{{/errors.phone}}
    </div>

    <div class="mt-3">
        {{#room.room_id}}<input type="hidden" name="room_id" value="{{room.room_id}}">{{/room.room_id}}
        <input type="hidden" name="action" value="{{action}}" />
        <input type="submit" value="{{action}}" class="btn btn-primary" />
    </div>

</form>
